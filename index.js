
/* const { mdLinks } = require('../mdLink');

mdLinks('README.md')
  .then(links => {
    console.log('Enlaces encontrados:', links);
  })
  .catch(err => {
    console.error('Error:', err);
  }); */
